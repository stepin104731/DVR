#include "factorial.h"

int checkv(int t)
{
    in(&p1,t);
    if(t==230)
        {
            i=1;
        return(0);
        }
    else if(t>230)
    {
        j=1;
        return(1);
    }
    else if(t<230)
    {
        k=1;
        return(-1);
    }
}
